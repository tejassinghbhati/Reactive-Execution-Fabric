### probes — List available probes

```bash
node cli.js probes
```

### stats — Show aggregate statistics

```bash
node cli.js stats
```

Displays total, enabled, and disabled job counts, plus the five most recent run records across all jobs.
